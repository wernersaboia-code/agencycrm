# Localização do Funil (PT/DE via cookie) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Catálogo, carrinho, detalhe da lista e checkout renderizam em PT ou DE conforme o idioma escolhido, resolvido por cookie.

**Architecture:** Cookie `NEXT_LOCALE` resolvido no `i18n/request.ts`; o layout do marketplace vira dinâmico e provê o locale via next-intl; strings extraídas para os namespaces `cart`/`catalog`/`checkout` em `messages/{pt,de}.json`; componentes server usam `getTranslations`, client usam `useTranslations`.

**Tech Stack:** Next.js 16 (App Router, RSC), next-intl v4, TypeScript, Vitest.

## Global Constraints

- Idiomas: `pt` (padrão) e `de`. Cookie: `NEXT_LOCALE`, valores aceitos `["pt","de"]`.
- Precedência de locale: `locale` explícito → cookie → `pt`.
- Nenhum link de navegação muda; a URL do funil não ganha prefixo de idioma.
- `formatCurrency` e `toLocaleString` de números permanecem; só texto é traduzido.
- Componentes server: `getTranslations("<ns>")`. Client: `useTranslations("<ns>")`.
- Plurais/contagens via ICU do next-intl.

### Glossário PT → DE (usar consistentemente)

- Carrinho → Warenkorb · Catálogo → Katalog · Lista(s) → Liste(n) · Leads → Leads
- Comprar → Kaufen · Checkout → Checkout · Finalizar compra → Kauf abschließen
- Pagamento seguro → Sichere Zahlung · Subtotal → Zwischensumme
- Filtros → Filter · Países → Länder · Setores → Branchen · Categoria → Kategorie
- Importadores → Importeure · Exportadores → Exporteure · Fabricantes → Hersteller
- Distribuidores → Distributoren · Varejistas → Einzelhändler · Atacadistas → Großhändler
- Empresas → Unternehmen · Emails verificados → Verifizierte E-Mails
- Ver detalhes → Details ansehen · Adicionar ao carrinho → In den Warenkorb
- Continuar comprando → Weiter einkaufen · Ver catálogo → Katalog ansehen

Países (código → DE): DE Deutschland · FR Frankreich · IT Italien · ES Spanien · NL Niederlande · BE Belgien · PL Polen · SE Schweden · AT Österreich · CH Schweiz · PT Portugal · GB Vereinigtes Königreich · US USA · CN China · JP Japan · BR Brasilien.

Setores (id → DE): food "Lebensmittel & Getränke" · tech "Technologie" · fashion "Mode & Textil" · automotive "Automobil" · health "Gesundheit & Pharma" · construction "Bau" · retail "Einzelhandel" · industrial "Industrie".

---

### Task 1: Resolução de locale por cookie + gravação

**Files:**
- Create: `lib/i18n/resolve-locale.ts`
- Test: `lib/i18n/resolve-locale.test.ts`
- Modify: `i18n/request.ts`
- Create: `actions/locale.ts`
- Create: `components/marketplace/sync-locale-cookie.tsx`

**Interfaces:**
- Produces:
  - `SITE_LOCALES = ["pt","de"] as const`, `type SiteLocale`, `DEFAULT_SITE_LOCALE = "pt"`
  - `resolveSiteLocale(explicit: string | undefined, cookieValue: string | undefined): SiteLocale`
  - `setLocaleCookie(locale: string): Promise<void>` (server action)
  - `<SyncLocaleCookie locale="pt"|"de" />` (client component)

- [ ] **Step 1: Write the failing test**

```ts
// lib/i18n/resolve-locale.test.ts
import { describe, it, expect } from "vitest"
import { resolveSiteLocale } from "./resolve-locale"

describe("resolveSiteLocale", () => {
    it("prefere o locale explícito válido", () => {
        expect(resolveSiteLocale("de", "pt")).toBe("de")
    })
    it("cai no cookie quando não há explícito", () => {
        expect(resolveSiteLocale(undefined, "de")).toBe("de")
    })
    it("ignora valores inválidos e usa o padrão pt", () => {
        expect(resolveSiteLocale("xx", "yy")).toBe("pt")
        expect(resolveSiteLocale(undefined, undefined)).toBe("pt")
    })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run lib/i18n/resolve-locale.test.ts`
Expected: FAIL — cannot find module.

- [ ] **Step 3: Implement resolve-locale**

```ts
// lib/i18n/resolve-locale.ts
export const SITE_LOCALES = ["pt", "de"] as const
export type SiteLocale = (typeof SITE_LOCALES)[number]
export const DEFAULT_SITE_LOCALE: SiteLocale = "pt"

function isSiteLocale(value: string | undefined): value is SiteLocale {
    return value === "pt" || value === "de"
}

export function resolveSiteLocale(
    explicit: string | undefined,
    cookieValue: string | undefined
): SiteLocale {
    if (isSiteLocale(explicit)) return explicit
    if (isSiteLocale(cookieValue)) return cookieValue
    return DEFAULT_SITE_LOCALE
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run lib/i18n/resolve-locale.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: Wire the cookie into `i18n/request.ts`**

Replace the body so it resolves via cookie when no explicit locale is passed:

```ts
// i18n/request.ts
import { getRequestConfig } from "next-intl/server"
import { cookies } from "next/headers"
import { resolveSiteLocale } from "@/lib/i18n/resolve-locale"

export default getRequestConfig(async ({ locale }) => {
    const cookieLocale = (await cookies()).get("NEXT_LOCALE")?.value
    const resolved = resolveSiteLocale(locale, cookieLocale)

    return {
        locale: resolved,
        messages: (await import(`../messages/${resolved}.json`)).default,
    }
})
```

- [ ] **Step 6: Create the server action**

```ts
// actions/locale.ts
"use server"

import { cookies } from "next/headers"
import { resolveSiteLocale } from "@/lib/i18n/resolve-locale"

export async function setLocaleCookie(locale: string): Promise<void> {
    const resolved = resolveSiteLocale(locale, undefined)
    ;(await cookies()).set("NEXT_LOCALE", resolved, {
        path: "/",
        sameSite: "lax",
        maxAge: 60 * 60 * 24 * 365,
    })
}
```

- [ ] **Step 7: Create the client cookie-sync component**

```tsx
// components/marketplace/sync-locale-cookie.tsx
"use client"

import { useEffect } from "react"

// Mantém o cookie NEXT_LOCALE alinhado com a landing exibida, para que o
// funil (que resolve o idioma pelo cookie) siga o mesmo idioma.
export function SyncLocaleCookie({ locale }: { locale: "pt" | "de" }) {
    useEffect(() => {
        const current = document.cookie
            .split("; ")
            .find((c) => c.startsWith("NEXT_LOCALE="))
            ?.split("=")[1]
        if (current !== locale) {
            document.cookie = `NEXT_LOCALE=${locale}; path=/; max-age=31536000; samesite=lax`
        }
    }, [locale])

    return null
}
```

- [ ] **Step 8: Typecheck + commit**

Run: `npx tsc --noEmit -p tsconfig.json` (exit 0).

```bash
git add lib/i18n/resolve-locale.ts lib/i18n/resolve-locale.test.ts i18n/request.ts actions/locale.ts components/marketplace/sync-locale-cookie.tsx
git commit -m "feat(i18n): resolução de locale do funil por cookie + gravação"
```

---

### Task 2: Layout dinâmico + seletor + pontos de sincronização

**Files:**
- Modify: `app/(marketplace)/layout.tsx`
- Modify: `components/marketplace/locale-switcher.tsx`
- Modify: `app/de/layout.tsx`
- Modify: `app/(marketplace)/page.tsx`

**Interfaces:**
- Consumes: `setLocaleCookie` (Task 1), `SyncLocaleCookie` (Task 1).

- [ ] **Step 1: Marketplace layout resolve o locale via next-intl**

Em `app/(marketplace)/layout.tsx`, trocar o provider fixo por resolução dinâmica:

```tsx
import { NextIntlClientProvider } from "next-intl"
import { getLocale, getMessages } from "next-intl/server"
// ...
export default async function MarketplaceLayout({ children }: { children: React.ReactNode }) {
    const locale = await getLocale()
    const messages = await getMessages()
    return (
        <NextIntlClientProvider locale={locale} messages={{ nav: messages.nav, cart: messages.cart }}>
            <CartProvider>
                <div className="min-h-screen flex flex-col">
                    <Suspense fallback={<div className="h-16 bg-background border-b" />}>
                        <MarketplaceHeader />
                    </Suspense>
                    <main id="main-content" className="flex-1">{children}</main>
                    <MarketplaceFooter locale={locale as "pt" | "de"} />
                    <CartDrawer />
                </div>
            </CartProvider>
        </NextIntlClientProvider>
    )
}
```

Remover o `import ptMessages` fixo. Nota: `getMessages()` retorna as mensagens do locale resolvido; passamos só `nav` e `cart` ao cliente.

- [ ] **Step 2: LocaleSwitcher grava o cookie ao trocar**

Em `components/marketplace/locale-switcher.tsx`, no `switchTo`, chamar a action antes de navegar:

```tsx
import { setLocaleCookie } from "@/actions/locale"
// ...
const switchTo = async (target: "pt" | "de") => {
    if (target === locale) return
    await setLocaleCookie(target)
    if (target === "de") {
        router.push(PT_TO_DE[pathname] ?? "/de")
    } else {
        router.push(DE_TO_PT[pathname] ?? "/")
    }
    router.refresh()
}
```
Tornar os `onClick` handlers `async` (ex.: `onClick={() => switchTo("de")}` já funciona com função async).

- [ ] **Step 3: Renderizar SyncLocaleCookie nas landings**

Em `app/de/layout.tsx`, dentro do `NextIntlClientProvider`, adicionar `<SyncLocaleCookie locale="de" />` (ao lado do `<HtmlLang lang="de" />`).

Em `app/(marketplace)/page.tsx` (home PT), adicionar no topo do JSX retornado `<SyncLocaleCookie locale="pt" />`.

Importar `SyncLocaleCookie` de `@/components/marketplace/sync-locale-cookie` nos dois arquivos.

- [ ] **Step 4: Typecheck**

Run: `npx tsc --noEmit -p tsconfig.json` (exit 0). (Nota: `messages.cart` ainda não existe até a Task 3 adicionar o namespace; se o typecheck reclamar do acesso, prossiga — a Task 3 adiciona a chave. Se `getMessages()` tipar como `AbstractIntlMessages`, o acesso `messages.cart` é permitido em runtime; caso o TS reclame, use `messages` inteiro: `messages={messages}`.)

- [ ] **Step 5: Commit**

```bash
git add "app/(marketplace)/layout.tsx" components/marketplace/locale-switcher.tsx app/de/layout.tsx "app/(marketplace)/page.tsx"
git commit -m "feat(i18n): layout do marketplace dinâmico + seletor grava cookie + sync nas landings"
```

---

### Task 3: Namespace `cart` + carrinho localizado

**Files:**
- Modify: `messages/pt.json`, `messages/de.json`
- Modify: `components/marketplace/cart-drawer.tsx`, `cart-item.tsx`, `add-to-cart-button.tsx`

**Interfaces:** Consumes: provider `cart` namespace (Task 2).

- [ ] **Step 1: Adicionar o namespace `cart`**

Em `messages/pt.json` (nível raiz):
```json
"cart": {
  "title": "Seu carrinho",
  "items": "{count, plural, one {# item} other {# itens}}",
  "empty": "Seu carrinho está vazio",
  "emptyHint": "Adicione listas de leads para começar",
  "seeCatalog": "Ver catálogo",
  "subtotal": "Subtotal",
  "review": "Revisar carrinho",
  "continue": "Continuar comprando",
  "securePayment": "Pagamento seguro via PayPal",
  "leads": "{count} leads",
  "addToCart": "Adicionar ao carrinho"
}
```
Em `messages/de.json`:
```json
"cart": {
  "title": "Ihr Warenkorb",
  "items": "{count, plural, one {# Artikel} other {# Artikel}}",
  "empty": "Ihr Warenkorb ist leer",
  "emptyHint": "Fügen Sie Lead-Listen hinzu, um zu starten",
  "seeCatalog": "Katalog ansehen",
  "subtotal": "Zwischensumme",
  "review": "Warenkorb prüfen",
  "continue": "Weiter einkaufen",
  "securePayment": "Sichere Zahlung über PayPal",
  "leads": "{count} Leads",
  "addToCart": "In den Warenkorb"
}
```

- [ ] **Step 2: Wire `cart-drawer.tsx`**

Adicionar `import { useTranslations } from "next-intl"`, `const t = useTranslations("cart")`, e substituir os literais: `Seu carrinho`→`{t("title")}`, o bloco de contagem → `{t("items", { count: itemCount })}` (removendo a lógica manual `item`/`itens`), `Seu carrinho está vazio`→`{t("empty")}`, `Adicione listas...`→`{t("emptyHint")}`, `Ver catálogo`→`{t("seeCatalog")}`, `Subtotal`→`{t("subtotal")}`, `Revisar carrinho`→`{t("review")}`, `Continuar comprando`→`{t("continue")}`, `Pagamento seguro via PayPal`→`{t("securePayment")}`.

- [ ] **Step 3: Wire `cart-item.tsx`**

`useTranslations("cart")`; substituir `{item.totalLeads.toLocaleString()} leads` por `{t("leads", { count: item.totalLeads })}` (usando `item.totalLeads.toLocaleString()` dentro? Não — passar o número; para manter separador de milhar, usar `t("leads", { count: item.totalLeads })` e no ICU o next-intl formata o número conforme o locale).

- [ ] **Step 4: Wire `add-to-cart-button.tsx`**

`useTranslations("cart")`; `Adicionar ao carrinho` → `{t("addToCart")}`.

- [ ] **Step 5: Verificação**

Run: `npx tsc --noEmit -p tsconfig.json` (exit 0) e `node -e "require('./messages/pt.json');require('./messages/de.json');console.log('ok')"`.

- [ ] **Step 6: Commit**

```bash
git add messages/pt.json messages/de.json components/marketplace/cart-drawer.tsx components/marketplace/cart-item.tsx components/marketplace/add-to-cart-button.tsx
git commit -m "feat(i18n): carrinho localizado (PT/DE)"
```

---

### Task 4: Namespace `catalog` + página e cards

**Files:**
- Modify: `messages/pt.json`, `messages/de.json`
- Modify: `app/(marketplace)/catalog/page.tsx`, `components/marketplace/catalog-stats.tsx`, `catalog-search.tsx`, `catalog-grid.tsx`, `list-card.tsx`

**Interfaces:** server components usam `getTranslations("catalog")`; client (`catalog-search`, `catalog-grid`, `list-card`) usam `useTranslations("catalog")` — exige que o provider inclua `catalog`. **Ajuste na Task 2:** incluir `catalog` no provider (`messages={{ nav: messages.nav, cart: messages.cart, catalog: messages.catalog }}`).

- [ ] **Step 1: Namespace `catalog` (pt)**

```json
"catalog": {
  "metaTitle": "Catálogo de Listas | Easy Prospect",
  "badge": "Bases prontas para prospecção B2B",
  "heroTitle": "Encontre a lista certa antes de gastar tempo com prospecção manual",
  "heroSubtitle": "Filtre por mercado, setor e categoria para comprar apenas os dados que fazem sentido para sua campanha.",
  "quickSearch": "Busca rápida",
  "searchPlaceholder": "Buscar por nome, país, setor ou descrição",
  "searchButton": "Buscar",
  "trustCompareTitle": "Compare",
  "trustCompareDesc": "Veja volume, países e setores antes de decidir.",
  "trustBuyTitle": "Compre com segurança",
  "trustBuyDesc": "Checkout via PayPal e pedido registrado na sua conta.",
  "trustDownloadTitle": "Baixe na hora",
  "trustDownloadDesc": "Acesse CSV ou Excel assim que o pagamento for confirmado.",
  "availableLists": "Listas disponíveis",
  "results": "{count, plural, one {# resultado} other {# resultados}} para os critérios atuais",
  "pageOf": "Página {page} de {pages}",
  "statListsFound": "Listas encontradas",
  "statLeadsOnPage": "Leads nesta página",
  "statActiveFilters": "Filtros ativos",
  "statCurrentPage": "Página atual",
  "emptyTitle": "Nenhuma lista combina com os filtros atuais",
  "emptyDesc": "Remova alguns filtros ou busque por um mercado mais amplo para encontrar listas disponíveis.",
  "clearFilters": "Limpar filtros",
  "featured": "Destaque",
  "companies": "{count} empresas",
  "verifiedEmails": "Emails verificados",
  "updatedAt": "Atualizada em {date}",
  "seeDetails": "Ver detalhes",
  "perLead": "{price} por lead",
  "defaultDescription": "Base pronta para prospecção com empresas qualificadas."
}
```

- [ ] **Step 2: Namespace `catalog` (de)**

```json
"catalog": {
  "metaTitle": "Listenkatalog | Easy Prospect",
  "badge": "Sofort einsatzbereite B2B-Datenbanken",
  "heroTitle": "Finden Sie die richtige Liste, bevor Sie Zeit mit manueller Recherche verlieren",
  "heroSubtitle": "Filtern Sie nach Markt, Branche und Kategorie und kaufen Sie nur die Daten, die zu Ihrer Kampagne passen.",
  "quickSearch": "Schnellsuche",
  "searchPlaceholder": "Nach Name, Land, Branche oder Beschreibung suchen",
  "searchButton": "Suchen",
  "trustCompareTitle": "Vergleichen",
  "trustCompareDesc": "Sehen Sie Umfang, Länder und Branchen, bevor Sie entscheiden.",
  "trustBuyTitle": "Sicher kaufen",
  "trustBuyDesc": "Checkout über PayPal, Bestellung in Ihrem Konto gespeichert.",
  "trustDownloadTitle": "Sofort herunterladen",
  "trustDownloadDesc": "Greifen Sie nach der Zahlungsbestätigung auf CSV oder Excel zu.",
  "availableLists": "Verfügbare Listen",
  "results": "{count, plural, one {# Ergebnis} other {# Ergebnisse}} für die aktuellen Kriterien",
  "pageOf": "Seite {page} von {pages}",
  "statListsFound": "Gefundene Listen",
  "statLeadsOnPage": "Leads auf dieser Seite",
  "statActiveFilters": "Aktive Filter",
  "statCurrentPage": "Aktuelle Seite",
  "emptyTitle": "Keine Liste passt zu den aktuellen Filtern",
  "emptyDesc": "Entfernen Sie einige Filter oder suchen Sie in einem breiteren Markt, um verfügbare Listen zu finden.",
  "clearFilters": "Filter zurücksetzen",
  "featured": "Empfohlen",
  "companies": "{count} Unternehmen",
  "verifiedEmails": "Verifizierte E-Mails",
  "updatedAt": "Aktualisiert am {date}",
  "seeDetails": "Details ansehen",
  "perLead": "{price} pro Lead",
  "defaultDescription": "Sofort einsatzbereite Datenbank mit qualifizierten Unternehmen."
}
```

- [ ] **Step 3: Wire os componentes**

- `catalog/page.tsx` (server): `const t = await getTranslations("catalog")`. Substituir `metadata` estático por `generateMetadata` que lê `getTranslations("catalog")` para o `metaTitle`. Trocar todos os literais (badge, heroTitle, heroSubtitle, quickSearch, trust*, availableLists, results via `t("results",{count:total})`, pageOf).
- `catalog-stats.tsx`: receber os labels já traduzidos OU tornar client com `useTranslations("catalog")` para `statListsFound/statLeadsOnPage/statActiveFilters/statCurrentPage`. Preferir: `const t = useTranslations("catalog")` (adicionar `"use client"`? é server hoje). Como é usado dentro de página server, passar os labels como props do server para evitar tornar client — mas o mais simples é adicionar `"use client"` e `useTranslations`. Escolha: adicionar `"use client"` em `catalog-stats.tsx` e usar `useTranslations("catalog")`.
- `catalog-search.tsx` (client): `useTranslations("catalog")` → `searchPlaceholder`, `searchButton`.
- `catalog-grid.tsx` (server hoje, sem estado) → precisa dos textos do estado vazio. Tornar `"use client"` OU passar labels via props. Escolha: `"use client"` + `useTranslations("catalog")` (emptyTitle/emptyDesc/clearFilters).
- `list-card.tsx` (client): `useTranslations("catalog")` → featured, companies via `t("companies",{count})`, verifiedEmails, updatedAt via `t("updatedAt",{date: updatedAt})`, seeDetails, perLead via `t("perLead",{price: formatCurrency(...)})`, defaultDescription, addToCart (reusar `cart.addToCart` — usar `useTranslations("cart")` também, ou duplicar em catalog; preferir `useTranslations("cart")("addToCart")`).

- [ ] **Step 4: Provider inclui `catalog`** — confirmar o ajuste citado nos Interfaces em `app/(marketplace)/layout.tsx`.

- [ ] **Step 5: Verificação + commit**

Run: `npx tsc --noEmit -p tsconfig.json` (exit 0) + validação JSON.
```bash
git add messages/pt.json messages/de.json "app/(marketplace)/catalog/page.tsx" components/marketplace/catalog-stats.tsx components/marketplace/catalog-search.tsx components/marketplace/catalog-grid.tsx components/marketplace/list-card.tsx "app/(marketplace)/layout.tsx"
git commit -m "feat(i18n): catálogo e cards localizados (PT/DE)"
```

---

### Task 5: Sidebar de filtros localizada (países/setores/categorias)

**Files:**
- Modify: `messages/pt.json`, `messages/de.json`
- Modify: `components/marketplace/catalog-sidebar.tsx`

**Interfaces:** client component; usa `useTranslations("catalog")` (namespace já no provider).

- [ ] **Step 1: Adicionar chaves ao namespace `catalog`**

Acrescentar em `catalog` (pt): `"filters": "Filtros"`, `"filtersHint": "Refine o catálogo por perfil da lista."`, `"countries": "Países"`, `"industries": "Setores"`, `"category": "Categoria"`, e mapas de nomes:
```json
"countryNames": { "DE":"Alemanha","FR":"França","IT":"Itália","ES":"Espanha","NL":"Holanda","BE":"Bélgica","PL":"Polônia","SE":"Suécia","AT":"Áustria","CH":"Suíça","PT":"Portugal","GB":"Reino Unido","US":"Estados Unidos","CN":"China","JP":"Japão","BR":"Brasil" },
"industryNames": { "food":"Alimentos & Bebidas","tech":"Tecnologia","fashion":"Moda & Têxtil","automotive":"Automotivo","health":"Saúde & Farmácia","construction":"Construção","retail":"Varejo","industrial":"Industrial" },
"categoryNames": { "importers":"Importadores","exporters":"Exportadores","manufacturers":"Fabricantes","distributors":"Distribuidores","retailers":"Varejistas","wholesalers":"Atacadistas" }
```
E em `catalog` (de): `"filters":"Filter"`, `"filtersHint":"Verfeinern Sie den Katalog nach Listenprofil."`, `"countries":"Länder"`, `"industries":"Branchen"`, `"category":"Kategorie"`, e:
```json
"countryNames": { "DE":"Deutschland","FR":"Frankreich","IT":"Italien","ES":"Spanien","NL":"Niederlande","BE":"Belgien","PL":"Polen","SE":"Schweden","AT":"Österreich","CH":"Schweiz","PT":"Portugal","GB":"Vereinigtes Königreich","US":"USA","CN":"China","JP":"Japan","BR":"Brasilien" },
"industryNames": { "food":"Lebensmittel & Getränke","tech":"Technologie","fashion":"Mode & Textil","automotive":"Automobil","health":"Gesundheit & Pharma","construction":"Bau","retail":"Einzelhandel","industrial":"Industrie" },
"categoryNames": { "importers":"Importeure","exporters":"Exporteure","manufacturers":"Hersteller","distributors":"Distributoren","retailers":"Einzelhändler","wholesalers":"Großhändler" }
```

- [ ] **Step 2: Wire `catalog-sidebar.tsx`**

Manter as constantes `COUNTRIES`/`INDUSTRIES`/`CATEGORIES` apenas com os `code`/`id` (remover o campo `name` fixo ou ignorá-lo). `const t = useTranslations("catalog")`. Renderizar nomes via `t(\`countryNames.${country.code}\`)`, `t(\`industryNames.${industry.id}\`)`, `t(\`categoryNames.${category.id}\`)`. Cabeçalhos: `t("filters")`, `t("filtersHint")`, `t("countries")`, `t("industries")`, `t("category")`, botão `t("clearFilters")`.

- [ ] **Step 3: Verificação + commit**

Run: `npx tsc --noEmit` (exit 0) + validação JSON.
```bash
git add messages/pt.json messages/de.json components/marketplace/catalog-sidebar.tsx
git commit -m "feat(i18n): sidebar de filtros do catálogo localizada (PT/DE)"
```

---

### Task 6: Namespace `checkout` + checkout/success/cancel

**Files:**
- Modify: `messages/pt.json`, `messages/de.json`
- Modify: `app/(marketplace)/checkout/page.tsx`, `checkout/success/page.tsx`, `checkout/cancel/page.tsx`

**Interfaces:** `/checkout` e `/cancel` são majoritariamente server (o `/checkout` tem partes client — o `PayPalButtonsWrapper` fica intacto). Usar `getTranslations("checkout")` em server; onde houver componente client de texto, `useTranslations("checkout")` (incluir `checkout` no provider se necessário — o checkout page é client (`"use client"`), então **incluir `checkout` no provider** em `app/(marketplace)/layout.tsx`).

- [ ] **Step 1: Namespace `checkout` (pt)**

```json
"checkout": {
  "back": "Voltar ao carrinho",
  "title": "Checkout",
  "subtitle": "Finalize o pagamento para liberar {count} leads em CSV e Excel.",
  "stepCart": "Carrinho",
  "stepPayment": "Pagamento",
  "stepDownload": "Download",
  "secureTitle": "Pagamento seguro",
  "secureDesc": "O PayPal processa o pagamento; o sistema libera a compra automaticamente após a confirmação.",
  "redirectNote": "Você será redirecionado no próprio fluxo do PayPal para autorizar a transação.",
  "trustSecureTitle": "Seguro",
  "trustSecureText": "Pagamento externo via PayPal.",
  "trustRegisteredTitle": "Registrado",
  "trustRegisteredText": "Pedido salvo para acesso futuro.",
  "trustImmediateTitle": "Imediato",
  "trustImmediateText": "Download liberado após aprovação.",
  "summary": "Resumo",
  "listsInOrder": "{count, plural, one {# lista no pedido} other {# listas no pedido}}",
  "totalLeads": "Total de leads",
  "total": "Total",
  "successMeta": "Compra Confirmada | Easy Prospect",
  "successTitle": "Compra confirmada",
  "successSubtitle": "Seu pedido foi aprovado e as listas já estão disponíveis para download.",
  "order": "Pedido",
  "leadsReleased": "Leads liberados",
  "totalPaid": "Total pago",
  "nextDownloadTitle": "Baixe",
  "nextDownloadText": "CSV ou Excel em Minhas Compras.",
  "nextProspectTitle": "Prospecção",
  "nextProspectText": "Importe leads no CRM e crie campanhas.",
  "nextEmailTitle": "Confirmação",
  "nextEmailText": "O recibo foi enviado para seu email.",
  "goToPurchases": "Acessar minhas compras",
  "useCrm": "Usar CRM",
  "keepShopping": "Continuar comprando",
  "cancelMeta": "Compra Cancelada | Easy Prospect",
  "cancelTitle": "Pagamento não finalizado",
  "cancelSubtitle": "Nenhuma cobrança foi concluída. Você pode revisar o carrinho ou tentar o checkout novamente.",
  "tryAgain": "Tentar novamente",
  "reviewCart": "Revisar carrinho",
  "backToCatalog": "Voltar ao catálogo"
}
```

- [ ] **Step 2: Namespace `checkout` (de)**

```json
"checkout": {
  "back": "Zurück zum Warenkorb",
  "title": "Checkout",
  "subtitle": "Schließen Sie die Zahlung ab, um {count} Leads als CSV und Excel freizuschalten.",
  "stepCart": "Warenkorb",
  "stepPayment": "Zahlung",
  "stepDownload": "Download",
  "secureTitle": "Sichere Zahlung",
  "secureDesc": "PayPal wickelt die Zahlung ab; das System gibt den Kauf nach der Bestätigung automatisch frei.",
  "redirectNote": "Sie werden im PayPal-Ablauf weitergeleitet, um die Transaktion zu autorisieren.",
  "trustSecureTitle": "Sicher",
  "trustSecureText": "Externe Zahlung über PayPal.",
  "trustRegisteredTitle": "Registriert",
  "trustRegisteredText": "Bestellung für späteren Zugriff gespeichert.",
  "trustImmediateTitle": "Sofort",
  "trustImmediateText": "Download nach Freigabe verfügbar.",
  "summary": "Übersicht",
  "listsInOrder": "{count, plural, one {# Liste in der Bestellung} other {# Listen in der Bestellung}}",
  "totalLeads": "Leads gesamt",
  "total": "Gesamt",
  "successMeta": "Kauf bestätigt | Easy Prospect",
  "successTitle": "Kauf bestätigt",
  "successSubtitle": "Ihre Bestellung wurde genehmigt und die Listen stehen zum Download bereit.",
  "order": "Bestellung",
  "leadsReleased": "Freigeschaltete Leads",
  "totalPaid": "Bezahlter Betrag",
  "nextDownloadTitle": "Herunterladen",
  "nextDownloadText": "CSV oder Excel unter Meine Käufe.",
  "nextProspectTitle": "Akquise",
  "nextProspectText": "Importieren Sie Leads ins CRM und erstellen Sie Kampagnen.",
  "nextEmailTitle": "Bestätigung",
  "nextEmailText": "Die Quittung wurde an Ihre E-Mail gesendet.",
  "goToPurchases": "Zu meinen Käufen",
  "useCrm": "CRM nutzen",
  "keepShopping": "Weiter einkaufen",
  "cancelMeta": "Kauf abgebrochen | Easy Prospect",
  "cancelTitle": "Zahlung nicht abgeschlossen",
  "cancelSubtitle": "Es wurde keine Zahlung abgeschlossen. Sie können den Warenkorb prüfen oder den Checkout erneut versuchen.",
  "tryAgain": "Erneut versuchen",
  "reviewCart": "Warenkorb prüfen",
  "backToCatalog": "Zurück zum Katalog"
}
```

- [ ] **Step 3: Wire as três páginas**

- `checkout/page.tsx` (client): `const t = useTranslations("checkout")`. Substituir todos os literais listados; `subtitle` via `t("subtitle",{count: totalLeads})`, `listsInOrder` via `t("listsInOrder",{count: items.length})`. Manter `formatCurrency` e o `PayPalButtonsWrapper`. Incluir `checkout` no provider (Task 2 layout).
- `checkout/success/page.tsx` (server): `const t = await getTranslations("checkout")`; `generateMetadata`/`metadata` → `successMeta`; substituir os literais (successTitle, successSubtitle, order, leadsReleased, totalPaid, next*, goToPurchases, useCrm, keepShopping).
- `checkout/cancel/page.tsx` (server): `const t = await getTranslations("checkout")`; `metadata`→`cancelMeta`; substituir cancelTitle, cancelSubtitle, tryAgain, reviewCart, backToCatalog.

- [ ] **Step 4: Verificação + commit**

Run: `npx tsc --noEmit` (exit 0) + validação JSON.
```bash
git add messages/pt.json messages/de.json "app/(marketplace)/checkout/page.tsx" "app/(marketplace)/checkout/success/page.tsx" "app/(marketplace)/checkout/cancel/page.tsx" "app/(marketplace)/layout.tsx"
git commit -m "feat(i18n): checkout, sucesso e cancelamento localizados (PT/DE)"
```

---

### Task 7: Detalhe da lista `/list/[slug]` localizado

**Files:**
- Read first: `app/(marketplace)/list/[slug]/page.tsx` e seus componentes de texto.
- Modify: `messages/pt.json`, `messages/de.json` (estender `catalog` ou novo namespace `listDetail`), e a página + componentes.

**Interfaces:** consome `getTranslations`/`useTranslations` conforme server/client de cada peça.

- [ ] **Step 1: Inventariar as strings**

Ler `app/(marketplace)/list/[slug]/page.tsx` e enumerar TODAS as strings PT visíveis (título, seções, rótulos de prévia, botões, badges). Listar cada uma no report antes de traduzir.

- [ ] **Step 2: Adicionar namespace `listDetail`**

Criar `listDetail` em `messages/{pt,de}.json` com as chaves inventariadas, traduzindo para DE seguindo o Glossário. (As traduções concretas dependem do inventário do Step 1 — segui-lo é obrigatório; usar o glossário para consistência de termos.)

- [ ] **Step 3: Wire a página + componentes**

Server → `getTranslations("listDetail")`; client → `useTranslations("listDetail")` (incluir no provider se houver componente client). Reusar `catalog`/`cart` para termos compartilhados (ex.: addToCart, companies, verifiedEmails).

- [ ] **Step 4: Verificação + commit**

Run: `npx tsc --noEmit` (exit 0) + validação JSON.
```bash
git add messages/pt.json messages/de.json "app/(marketplace)/list/[slug]"
git commit -m "feat(i18n): página de detalhe da lista localizada (PT/DE)"
```

---

## Self-Review

**Cobertura da spec:** cookie/resolução → Task 1; layout dinâmico + seletor + sync → Task 2; carrinho → Task 3; catálogo (página+cards+stats+search+grid) → Task 4; sidebar/filtros → Task 5; checkout/success/cancel → Task 6; detalhe da lista → Task 7. Todos os itens do escopo da spec têm task.

**Placeholders:** As Tasks 1–6 trazem strings e traduções concretas. A Task 7 é a única com conteúdo derivado de leitura em tempo de implementação (o arquivo `/list/[slug]` não foi inventariado no planejamento) — está marcada explicitamente com o passo de inventário e a obrigação de seguir o Glossário; não é um "TODO" vago, e sim um passo de extração definido.

**Consistência:** o provider do layout acumula os namespaces client conforme as tasks avançam (`nav`,`cart` na Task 2; `+catalog` na Task 4; `+checkout` na Task 6) — cada task que adiciona um namespace client ajusta o provider no mesmo commit.

**Nota de execução:** as Tasks 3–7 tocam `messages/{pt,de}.json` em sequência; ao executar em paralelo haveria conflito. Executar as tasks em ordem (não concorrentemente) por causa do arquivo de mensagens compartilhado.
